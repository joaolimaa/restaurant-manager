services:
  app:
    container_name: restaurant-manager
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://restaurante-manager-app.postgres.database.azure.com:5432/restaurant-manager
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: Asd123!.
    networks:
      - restaurant-manager-network

volumes:
  restaurant-manager-data:

networks:
  restaurant-manager-network:
